.env
.env.local
.env.*.local
node_modules/
.DS_Store

# Note: .claude/agents/, .claude/commands/, .claude/skills/, and .claude/hooks/
# ARE committed — they hold project-specific reviewers, slash commands, skills,
# and harness hooks that should ship with the repo.

# Local Claude Code state
.claude/settings.local.json
.playwright-mcp/
