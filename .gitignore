.env
.env.local
.env.*.local
node_modules/
.DS_Store

# Vendored Claude Code hooks (installed per-machine)
.claude/hooks/

# Note: .claude/agents/, .claude/commands/, and .claude/skills/ ARE committed
# — they hold project-specific reviewers, slash commands, and skills.

# Local Claude Code state
.claude/settings.local.json
.playwright-mcp/
